# 複製成 .env.local 後填入實際的值。這個範本檔會進版控，.env.local 不會。

# 中央氣象署開放資料平臺授權碼（設計架構書 §9）
# 申請：https://opendata.cwa.gov.tw/ 免費註冊即可取得
# 用途：鄉鎮天氣預報 F-D0047 系列，逐 3 小時降雨機率與體感溫度
# 只在伺服器端使用，透過 app/api/weather/route.ts 代理，不得暴露到瀏覽器
CWA_API_KEY=

# Google Routes API 金鑰（ADR-0005）
# 申請：Google Cloud Console → 啟用 Routes API → 建立 API 金鑰
# 注意：即使用量在免費額度（Compute Route Matrix 每月 10,000 個元素）內，
#      Google 仍要求專案綁定帳單帳戶才能啟用 API。
# 用途：取得即時路況車程，覆蓋 Place.driveMinutes 這個基準值
# 沒設也能跑：此時一律使用建檔時填的 driveMinutes（P6 離線可用）
GOOGLE_ROUTES_API_KEY=

# Web Push 的 VAPID 金鑰（設計架構書 §9.4）
# 產生：node scripts/generate-vapid-keys.mjs（貼上輸出的兩行）
# 用途：瀏覽器訂閱推播、伺服器送出推播時的身分憑證
# 沒設也能跑：訂閱卡片會誠實顯示「伺服器還沒設定推播金鑰」，其餘功能不受影響
#
# ⚠️ 公鑰**刻意不用 NEXT_PUBLIC_ 前綴**。那是建置期替換，而映像是
#    GitHub Actions 建的、那裡沒有這個檔案，結果會是瀏覽器拿到 undefined。
#    公鑰改由伺服器元件在請求時讀出來傳給前端（lib/push/vapid.ts）。
#
# ⚠️ 換金鑰會讓所有既有訂閱失效，換完要清空 push_subscriptions 資料表。
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
# 推播服務用來聯絡站台管理者的位址，必須是 mailto: 或 https:。
# 留空則使用站台位址。
VAPID_SUBJECT=

# SQLite 檔案位置。留空則使用 ./data/kidgo.db
DATABASE_URL=
