# 開發約定

這份文件存在的唯一目的：**讓兩年後接手的人（可能是你自己）看得懂當初為什麼這樣做。**

程式碼說明「做了什麼」，git history 與 ADR 說明「為什麼」。後者才是會遺失的那一半。

---

## 1. commit 訊息寫「為什麼」

格式：`<type>: <一句話結果>`，空行，然後是**理由**。

可用的 type：`feat` / `fix` / `refactor` / `test` / `docs` / `chore` / `tune`。

`tune` 是本專案特有的，專門用於調整 `thresholds.ts` 或 `weights.ts`。
這類 commit **必須寫出觸發調整的實際觀察**，否則半年後沒人知道 0.5 是怎麼來的。

```
tune: 天氣權重 0.3 → 0.5

三次雨天實測，Stage 1 過濾後仍有純戶外地點排進前三。
降雨機率 40–60% 的區間權重不足，戶外地點靠作息契合度就能補回來。

對應測試：lib/recommend/__tests__/scoring.test.ts 的「中度降雨機率壓低戶外地點」
```

反例（不要這樣寫）：`調整權重`、`fix bug`、`update`。

**一個功能一個 commit。** 不要把 schema 變更、UI 調整、依賴升級混在同一個 commit。

---

## 2. ADR：只在「有替代方案被否決」時才寫

放在 `docs/adr/`，格式見 [`docs/adr/TEMPLATE.md`](docs/adr/TEMPLATE.md)。

判準只有一個：

> **這個決定有沒有一個合理的替代方案被你否決掉？**

| 情況 | 要寫 ADR 嗎 |
|------|------------|
| 選 SQLite 而不是 Supabase | ✅ 要——未來一定有人問「為什麼不用雲端」 |
| 決策層禁用 LLM | ✅ 要——這違反直覺，不寫一定會被人「優化」掉 |
| 用 TypeScript | ❌ 不用——沒有被認真考慮的替代方案 |
| 把按鈕改成藍色 | ❌ 不用 |

照這個標準，整個 v1 大概只會產生 5–8 份 ADR。**寫太多等於沒寫。**

ADR 一旦寫下就不修改內容。決定變了就寫一份新的，把舊的狀態改成 `已被 ADR-XXXX 取代`。
這樣才看得到想法的演變過程。

---

## 3. 推薦引擎的測試就是規格書

`lib/recommend/` 是純函式，**每一條決策規則都必須有一個對應的測試**。

測試名稱寫成**判斷句**，讓人讀測試檔就知道系統編碼了哪些育兒常識：

```ts
it("降雨機率 61% 時，純戶外地點被剔除", () => { ... });
it("可用時間不足以在午睡前回到家時，地點被剔除", () => { ... });
it("多個小孩時取最低分，不取平均", () => { ... });
```

不要寫 `it("should work correctly")` 這種名字——那等於沒有規格。

設計架構書 §13 要求至少涵蓋：雨天過濾、午睡時間衝突、多小孩取最低分、
最近去過降權、歷史成效權重不超過 5%。

```bash
npm test
```

---

## 4. 改資料模型前先討論

`Place` 的欄位定義是本產品的核心差異化。設計架構書 §5.2 的每個欄位都有理由
（「只記錄 Google Maps 和現有懶人包查不到的東西」）。

要增刪欄位，先在 issue 或對話中講清楚，取得同意後才動 schema，並寫進 ADR。

---

## 5. 已知取捨

| 項目 | 狀態 |
|------|------|
| `npm audit` 有 4 個 moderate | 來自 `drizzle-kit` 的傳遞依賴 `esbuild`（開發伺服器 CORS 問題）。僅影響 devDependency，`audit fix --force` 會把 drizzle-kit 降到 0.18.1，不值得。等上游修。 |
| `AGENTS.md` 開頭的區塊 | 由 `next dev` 自動寫入與維護，不要手動刪除，會被重新加回來。專案自己的規則寫在分隔線以下。 |
